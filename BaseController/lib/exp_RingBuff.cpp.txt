#include <iostream>
#include "RingBuff.h"
using namespace std;
int main()
{
	int a, b, c;
	char t[255] = {'\0'};
	RingBuff buff(300);
	buff.Put_s("123,45,dgg|345,34,45|sgakkl,ert,ywert|981,1234,98|\0\0\0\0|akgikl");
	buff.Get_Frame(t);
	cout << t << endl;
	sscanf(t, "%d,%d,%d", &a, &b, &c);
	cout << a << '\t' << b << '\t' << c << endl;
	buff.Get_Frame(t);
	sscanf(t, "%d,%d,%d", &a, &b, &c);
	cout << a << '\t' << b << '\t' << c << endl;
	buff.Get_Frame(t);
	sscanf(t, "%d,%d,%d", &a, &b, &c);
	cout << a << '\t' << b << '\t' << c << endl;
	buff.Get_Frame(t);
	sscanf(t, "%d,%d,%d", &a, &b, &c);
	cout << a << '\t' << b << '\t' << c << endl;
}